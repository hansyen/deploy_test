apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: deploy-test
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: deploy-test
    spec:
      containers:
      - name: deploy-test
        image: ohya/<image>
        resources:
          limits:
            memory: 0
            cpu: 0
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          protocol: TCP
      restartPolicy: Always
